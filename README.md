# Ocean Rescue — AI Studio Handoff

Disposable Google AI Studio workspace for AidenGame Ocean Rescue.

- Canonical repository: `savior714/game`
- Admitted source base: `f984670efa538aa679f65bea729f03dafec5fa66`
- This repository is **not** canonical product history.
- `npm run dev` materializes `domains/ocean-rescue` from the exact source base into `workspace/`, installs its pinned npm dependencies without writing a lockfile, and starts the existing Vite development server on port 5173.
- Product and architecture references are copied to `reference/`.
- Do not push AI-generated gameplay back to `savior714/game` from AI Studio. Export/download the AI Studio result for separate review and integration.

The editable game root inside AI Studio is `workspace/`.
